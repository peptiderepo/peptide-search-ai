name: Deploy to WordPress

on:
  push:
    branches: [ main ]

permissions:
  contents: read

concurrency:
  group: deploy-peptide-search-ai
  cancel-in-progress: true

jobs:
  test:
    name: Pre-deploy tests
    uses: ./.github/workflows/ci.yml

  validate:
    name: Validate deployment
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Verify required files exist
        run: |
          for file in peptide-search-ai.php includes/ assets/ readme.txt; do
            [ -e "$file" ] && echo "✓ $file" || { echo "❌ Missing: $file"; exit 1; }
          done
      - name: Verify PSA_VERSION constant
        run: |
          if grep -q "define( 'PSA_VERSION'" peptide-search-ai.php; then
            version=$(grep "define( 'PSA_VERSION'" peptide-search-ai.php | sed "s/.*'\([^']*\)'.*/\1/")
            echo "✓ PSA_VERSION: $version"
          else
            echo "❌ PSA_VERSION not found"; exit 1
          fi
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.1'
      - name: Validate PHP syntax
        run: find . -name '*.php' -not -path './vendor/*' -print0 | xargs -0 -n1 php -l > /dev/null

  deploy:
    name: Deploy (shared pipeline)
    needs: validate
    uses: peptiderepo/peptide-e2e/.github/workflows/deploy-app.yml@main
    with:
      target_path: wp-content/plugins/peptide-search-ai
    secrets: inherit
